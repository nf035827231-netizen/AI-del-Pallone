// Settlement is based on Football-Data.org match IDs saved by /api/pick.
const FD_BASE='https://api.football-data.org/v4';
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');const id=String(req.query?.id||'').replace(/^football-data-/,'');const token=process.env.FOOTBALL_DATA_TOKEN||'';if(!id||!/^[0-9]+$/.test(id))return res.status(400).json({error:'ID partita mancante'});if(!token)return res.status(500).json({error:'FOOTBALL_DATA_TOKEN non configurato'});try{const r=await fetch(`${FD_BASE}/matches/${id}`,{headers:{'X-Auth-Token':token}});if(!r.ok)return res.status(r.status).json({error:`Football-Data.org HTTP ${r.status}`});const m=await r.json();return res.status(200).json(m);}catch(e){return res.status(500).json({error:e.message});}}
